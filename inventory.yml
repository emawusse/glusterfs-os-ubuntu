#Managers
managers:
  hosts:
    manager1: 
      ansible_host: x

  vars:
    ansible_become: yes
    ansible_ssh_user: x
    ansible_ssh_private_key_file: x

#Workers
workers:
  hosts:
    worker1:
      ansible_host:  x
    worker2:
      ansible_host: x
    worker3:
      ansible_host: x
  vars:
    ansible_become: yes
    ansible_ssh_user: x
    ansible_ssh_private_key_file: x

#Noeuds
Noeuds:
  children:
    managers:
    workers: